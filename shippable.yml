
# Language setting
language: go

# Version number
go:
  #- 1.1
  - 1.2
  - 1.3
env:
  - foo=bar
  - test=foo
matrix:
  include:
    - go: 1.1
  exclude:
    - go: 1.2
  allow_failures:
    - go: 1.3
build:
  ci:
    - go version
    - if [ "$SHIPPABLE_GO_VERSION" == "1.3" ]; then foobar; fi
    
    
    
    
    
